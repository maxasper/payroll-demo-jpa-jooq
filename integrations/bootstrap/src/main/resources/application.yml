spring:
  datasource:
    url: ${JDBC_URL:jdbc:postgresql://localhost:5432/payroll}
    username: ${DB_USERNAME:payroll}
    password: ${DB_PASSWORD:payroll}
  jpa:
    hibernate:
      ddl-auto: none
  jooq:
    sql-dialect: postgres
  flyway:
    enabled: true
    locations: classpath:db/migration
